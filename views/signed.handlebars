<h1>Thank you for signing up!!</h1>
<img src="{{ image }}" alt="signature" width="350" height="100">
<h3>See all other <a href="/signers">{{ num }} signers</a>!</h3>
    <form method="POST">
        <input type="hidden" name="_csrf" value="{{csrfToken}}">
        <button id="delete-button" class="buttons">Delete your signature</button>
    </form>