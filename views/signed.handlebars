<h1>Thank you for signing up!! 🙌</h1>
<img id="signature-box" src="{{ image }}" alt="signature" width="350" height="100" />
<p id="signers-message">
    See all other <a href="/signers">{{ num }} signers</a>!
</p>
<form method="POST">
    <input type="hidden" name="_csrf" value="{{ csrfToken }}" />
    <button id="delete-button" class="buttons">Delete your signature 😕</button>
</form>
